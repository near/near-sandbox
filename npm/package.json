{
  "name": "near-sandbox-tools",
  "description": "CLI tool for testing NEAR smart contracts",
  "repository": "https://github.com/near/sandbox",
  "author": "Willem Wyndham (willem@near.org)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn workspace near-sandbox-runner run build",
    "test": "yarn workspaces run test"
  },
  "workspaces": [
    "runner",
    "sandbox",
    "tests/*"
  ]
}
